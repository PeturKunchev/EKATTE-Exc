Документация на проекта: EKATTE Search System

1. Описание на проекта

Целта на проекта е разработване на уеб приложение, което позволява търсене на населени места от българския Единен Класификатор на Административно-Териториалните и Териториални Единици (ЕКАТТЕ). Приложението импортира данни от официалните EKATTE JSON файлове в PostgreSQL база данни, предоставя API за търсене по дума и визуализира резултатите чрез уеб интерфейс.

2. Технологии

Backend
Node.js (ES Modules)
PostgreSQL
pg (PostgreSQL клиент за Node)
vitest, c8 (unit тестове)

Frontend
Vanilla JS
Fetch API
Responsive layout


3. Описание на базата данни

Използват се четири таблици, които покриват официалната структура на EKATTE:

1. regions
Съдържа областите.

2. municipalities
Съдържа общините със връзка към област.

3. mayoralties
Съдържа кметствата (ако има). Някои населени места не са в кметство → mayoralty_id = NULL.

4. settlements
Основната таблица → всички населени места.

Нормализация
Базата е нормализирана до 3-та нормална форма (3NF):
липсват транзитивни зависимости
липсват частични зависимости
използват се външни ключове за коректна структура

4. Импорт на EKATTE данните

Всеки файл се валидира чрез модул /validators/validators.js.

Примери за валидатори:

isValidEkatte("12345") → true  
isValidRegionCode("BLG") → true  
isValidMunicipalityCode("VAR01") → true  
isValidMayoraltyCode("VAR01-02") → true


При импорта се проверяват:
формат на кодовете
валидни документни номера
налични задължителни полета
Невалидните записи се пропускат.

5. Backend обработка на заявките

API-то има един единствен публичен ендпойнт, както е описано в заданието:

🔎 GET /api/search?query=...

Търси населени места по дума, като връща:

id / ekatte
име на селището
кметство (ако няма → името на селището)
община
област

статистика за:
брой намерени селища
различни кметства
различни общини
различни области

Пример отговор:
{
  "query": "со",
  "stats": {
    "settlements": 2,
    "mayoralties": 1,
    "municipalities": 1,
    "regions": 1
  },
  "results": [
    {
      "id": 1,
      "ekatte_code": "68134",
      "settlement_name": "София",
      "mayoralty_name": "",
      "municipality_name": "Столична",
      "region_name": "София-град"
    }
  ]
}

6. Frontend интерфейс

При зареждане на страницата:

извежда статистика (брой записи)
поддържа търсене по ключова дума
показва таблица с резултатите
ако няма кметство → показва името на населеното място
Frontend-ът се базира на:
fetch() към /api/search
динамично обновяване на DOM
responsive CSS

7. Unit тестове (100% coverage)

Проектът съдържа тестове за:

1) validators.js

Покрива 100% от функциите
Всички валидатори са тествани с валидни и невалидни стойности

2) helpers/

setCors.js
sendJson.jS
responses.js
Също покрити 100%.


8. Начин на работа

1️⃣ Стартира се PostgreSQL база
2️⃣ Изпълнява се 01_tables.sql
3️⃣ Стартират се importer скриптовете:

node import/importDocuments.js  
node import/importRegions.js  
node import/importMunicipalities.js  
node import/importMayoralities.js  
node import/importSettlements.js  


4️⃣ Стартира се сървърът:

node server.js


5️⃣ Фронтендът се отваря в браузър:

frontend/index.html

9. Използвани библиотеки

Библиотека	Причина
pg	Комуникация с PostgreSQL
dotenv	Работа с environment променливи
c8, vitest	Unit тестване
node:http	Лек custom сървър без Express
Fetch API (frontend)	AJAX заявки
